# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Icon design drafts (scratch)
icon-drafts/

# Local docs / design specs (not tracked)
docs/

# Updater signing keys — private keys must never be committed (they live in
# ~/.tempoterm). These patterns are a safety net if one is ever copied in.
*.key
updater-key*.json

# Claude Code local state (settings, worktrees, transcripts, etc.)
.claude/
